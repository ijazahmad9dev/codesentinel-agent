services:
  codesentinel:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: codesentinel-agent
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./project:/app/project
    stdin_open: true
    tty: true